const list = [];
const anotherList = list.concat(157);

const persona = {
	name: 'Miguel',
	twitter: '@midudev',
	age: 18,
	isDeveloper: true,
	links: ['htpps://midu.tube', 'https://midu.live'],
};

const field = 'twitter';

console.log(persona[field]);
